version: "3"

services:
    web-server:
        image: natsuwumi/these-waifus-also-dont-exist:latest
        networks:
          - proxy_net
        volumes:
          - ./img:/srv/these-waifus-also-dont-exist/img
        environment:
          - RUST_LOG=info

networks:
  proxy_net:
    external: true
